# URL of the aggregator hub WebSocket the browser connects to.
# Local dev default. For production point this at your Railway deployment,
# e.g. wss://your-app.up.railway.app
NEXT_PUBLIC_HUB_URL=ws://localhost:8080
